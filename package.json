{
  "name": "desmoche",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm run build -w shared",
    "build:server": "npm run build:shared && npm run build -w server",
    "build:client": "npm run build:shared && npm run build -w client",
    "start:server": "npm run start -w server",
    "start:client": "npm run start -w client",
    "test": "npm run test -w shared && npm run build:shared && npm run test -w server",
    "test:watch": "npm run build:shared && npm run test:watch -w server",
    "dev": "npm run build:shared && npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
